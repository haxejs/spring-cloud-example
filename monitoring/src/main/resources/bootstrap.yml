spring:
  application:
    name: monitoring
  # cloud:
  #   config:
  #     uri: ${CONFIG_SERVER_URL:http://localhost:8710}
      # fail-fast: true
      # password: ${CONFIG_SERVICE_PASSWORD}
      # username: user